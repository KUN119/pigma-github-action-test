name: Sync Design Tokens

# 언제 실행될지 정의
on:
  push:
    # 'develop' 브랜치에 푸시될 때만
    branches:
      - develop
    # 중요: 'tokens' 폴더 내의 json 파일이 변경되었을 때만 실행 (불필요한 실행 방지)
    paths:
      - "tokens/**.json"

permissions:
  contents: write # 실행할 작업 권한 설정 (커밋을 위해 쓰기 권한 필요)

jobs:
  build_and_commit:
    runs-on: ubuntu-latest
    steps:
      # 1. 코드 내려받기
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Node.js 환경 설정
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20" # 현재 LTS 버전 권장

      # 3. 의존성 설치 (npm ci는 CI 환경에서 더 빠르고 안정적입니다)
      - name: Install Dependencies
        run: npm ci

      # 4. 아까 만든 빌드 스크립트 실행 (JSON -> CSS 변환)
      - name: Build Design Tokens
        run: npm run build:tokens

      # 5. 변경된 CSS 파일 감지 및 자동 커밋
      - name: Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🎨 Design System Update: Auto-generated CSS"
          # 중요: 변환된 결과물 파일 경로 (아까 build.js에서 설정한 경로)
          file_pattern: "src/styles/variables.css"
