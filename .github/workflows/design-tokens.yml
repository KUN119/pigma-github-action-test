name: Sync Design Tokens

on:
  push:
    branches:
      - develop
    paths:
      - "tokens/tokens.json" # 원본 토큰 변경 시 실행

permissions:
  contents: write

jobs:
  build_and_commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Dependencies
        run: npm ci

      # 1) 토큰 변환 실행 (build.js 실행)
      - name: Generate Design Tokens
        run: npm run build

      # 2) CSS 파일만 자동 커밋
      - name: Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🎨 Auto-update: design tokens synced"
          file_pattern: "src/styles/variables.css"
